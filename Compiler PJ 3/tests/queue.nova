import <collections> ;
{
    Queue q ;
    q.init() ;
    q.enqueue(5) ; q.enqueue(15) ; q.enqueue(25) ;
    print(q.size()) ;
    print(q.isEmpty()) ;
    print(q.dequeue()) ;
    print(q.dequeue()) ;
    print(q.size()) ;
    print(q.isEmpty()) ;
    print(q.dequeue()) ;
    print(q.isEmpty()) ;
}
